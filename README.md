# 🧮 Nginx Log Analyser

A simple shell script that analyzes **Nginx access logs** and provides key traffic insights directly from the command line.

---

## 📋 Features

The script extracts and displays:
- Top **5 IP addresses** with the most requests  
- Top **5 most requested paths (URLs)**  
- Top **5 HTTP response status codes**  
- Top **5 user agents (browsers/clients)**  


## 🧰 Requirements

This tool uses standard Linux command-line utilities, available by default on most systems:
- `awk`
- `sort`
- `uniq`
- `head`

No extra installations required 


## 🚀 Usage

1. Clone the repository:
   ```bash
   git clone https://github.com/l1nzey/Nginx-Log-Analyser.git
   cd Nginx-Log-Analyser

   ```
   
   https://roadmap.sh/projects/nginx-log-analyser


